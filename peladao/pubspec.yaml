name: peladao1
description: Peladão App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.3.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/ # Referencia o diretório inteiro
    # - assets/images/logo.png # Ou arquivos específicos
    # - assets/images/icones.png
    # - assets/images/default_profile.png

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # TODO: Adicionar outras dependências necessárias (ex: image_picker, http, provider, firebase_core, firebase_auth, cloud_firestore, firebase_storage, google_mobile_ads, pdf, path_provider, etc.)
  google_mobile_ads: ^5.1.0 # Adicionado para AdMob - verificar versão mais recente se necessário

  pdf: ^3.6.5 # Para gerar PDF
  printing: ^5.9.0 # Para visualizar/imprimir PDF
  # firebase_messaging: ^14.6.7 # Para notificações Push (Requer configuração Firebase)
  image_picker: ^1.0.0 # Para selecionar fotos

